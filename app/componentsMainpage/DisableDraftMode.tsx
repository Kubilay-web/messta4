"use client";

// Sanity draft mode (next-sanity) kaldırıldı. Sanity kullanılmadığı için bu
// bileşen artık bir şey render etmez; yapıyı bozmamak için no-op olarak durur.
const DisableDraftMode = () => {
  return null;
};

export default DisableDraftMode;
